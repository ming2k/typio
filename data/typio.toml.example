# Typio configuration example

default_engine = "basic"

[engines.rime]
schema = "luna_pinyin"
page_size = 9
popup_theme = "auto"
candidate_layout = "horizontal"
font_size = 11
# shared_data_dir = "/usr/share/rime-data"
# user_data_dir = "~/.local/share/typio/rime"
# full_check = false

[engines.mozc]
page_size = 9
# server_path = "/usr/lib/mozc/mozc_server"

# Voice input configuration
# backend: "whisper" (whisper.cpp) or "sherpa-onnx"
# For whisper: model is ggml model name (base, small, medium, large)
#   place model at ~/.local/share/typio/whisper/ggml-<model>.bin
# For sherpa-onnx: model is the directory name under ~/.local/share/typio/sherpa-onnx/
#   supports transducer (Zipformer), paraformer, whisper ONNX, nemo-ctc
[voice]
# backend = "sherpa-onnx"
# language = "auto"   # omit or "auto" for auto-detect; or "zh", "en", "ja", etc.
# model = "default"

# Keyboard shortcuts
[shortcuts]
# switch_engine = "Ctrl+Shift"
# voice_ptt = "Super+v"

# Legacy whisper-only config (still supported, overridden by [voice] if present)
[whisper]
# language = "zh"
model = "base"
